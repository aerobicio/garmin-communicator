box: wercker/nodejs
build:
  steps:
    - npm-install
    - script:
        name: spec
        code: make spec
    - script:
        name: build
        code: |
          make build
          cp garmin*.js $(WERCKER_REPORT_ARTIFACTS_DIR)
